/*
package com.mygdx.pmd.Runner;

import com.mygdx.pmd.Controller.Controller;
import com.mygdx.pmd.Controller.TestingController;
import com.mygdx.pmd.Timer;
import com.mygdx.pmd.View.ImageLibrary;

public class PokemonRunner {
    private Controller beginningController;
    Timer timer;
    final ImageLibrary imageLibrary;
    //final OverlayStorage overlayStorage;

    public static void main(String[] args) {
        PokemonRunner runner = new PokemonRunner();
        runner.run();
    }

    public PokemonRunner() {
        imageLibrary = new ImageLibrary();
        window = new WindowInitializer();
        beginningController = new TestingController();
        window.initialize(beginningController);
        timer = new Timer(beginningController);
    }

    public void run() {
        timer.ticking(window);
    }


}*/
